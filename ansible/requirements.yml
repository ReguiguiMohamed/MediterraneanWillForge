---
# ansible/requirements.yml
# Galaxy collections required by all roles.
# Installed by bootstrap.sh before ansible_local provisioner runs.

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: community.docker
    version: ">=3.10.0"
